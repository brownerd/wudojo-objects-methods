// String.prototype.anchor()
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/anchor

export const anchorMaker = (str, anchorName) => str.anchor(anchorName)

// Not sure if or when I would ever need this, but here it is
